org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# The Windows user profile path on this machine contains a non-ASCII
# character (ç), which trips the Android Gradle Plugin's advisory
# non-ASCII-path check. The check is a legacy Windows compatibility
# warning (https://issuetracker.google.com/36903609); overriding it is
# the officially documented workaround.
android.overridePathCheck=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
